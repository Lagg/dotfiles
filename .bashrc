alias ls="ls --all --color=always"
alias pstree="pstree -p -l"
alias less="less -R"
alias info="info --vi-keys"

export PS1='\u@\H[\W]\$ '
export PAGER="less -R"
export EDITOR="vim"
export VISUAL="$EDITOR"
